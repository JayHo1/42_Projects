#include "computer.h"

int			main(int argc, char **argv)
{
	if (argc == 2)
	{
		char 	**str;

		str = ft_parse(argv[1]);
		// while (str[i])
		// {
		// 	if (i == 0 )
		// 		printf("First string: %s\n", str[i]);
		// 	else
		// 		printf("Second string: %s\n\n", str[i]);
		// 	i++;
		// }
		// resolve_first_degree(13, 9.3);
	}
	return (0);
}